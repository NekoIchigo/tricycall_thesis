import 'package:flutter/material.dart';
import 'package:get/get.dart';

class ShowErrorDialog extends StatelessWidget {
  const ShowErrorDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
